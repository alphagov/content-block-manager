<legend class="govuk-fieldset__legend govuk-fieldset__legend--m">
  <%= I18n::t("edition.titles.#{block_type}.#{field_name}") %>
</legend>

<%= render("components/datetime_fields", {
  heading_size: "s",
  field_name: field_name,
  prefix: name_prefix,
  date_heading: "Date",
  date_hint: "For example, 01 08 2025",
  time_hint: "For example, 09:30 or 19:30",
  error_items:,
  date_id: date_field.id_attribute,
  year: {
    value: date_time.year,
    id: "#{field_name}_year",
    name: field_name_for("year"),
    label: "Year",
    width: 4,
  },
  month: {
    value: date_time.month,
    id: "#{field_name}_month",
    name: field_name_for("month"),
    label: "Month",
    width: 2,
  },
  day: {
    value: date_time.day,
    id: "#{field_name}_day",
    name: field_name_for("day"),
    label: "Day",
    width: 2,
  },
  hour: {
    value: date_time.hour&.to_i,
    id: "#{field_name}_hour",
    name: field_name_for("hour"),
  },
  minute: {
    value: date_time.min&.to_i,
    id: "#{field_name}_minute",
    name: field_name_for("minute"),
  },
}) %>
