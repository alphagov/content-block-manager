<%= form_with(
      url: edition_path(
        @edition,
        redirect_path:,
      ),
      method: :delete,
    ) do %>
  <%= render "govuk_publishing_components/components/button", {
    text: I18n.t("buttons.delete_draft"),
    type: "submit",
    destructive: true,
  } %>
<% end %>
