<% content_for :context, @edition.title %>
<% content_for :title, "Reorder" %>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-full">
    <%= render Edition::ReorderComponent.new(edition: @edition, order: @order) %>
  </div>
</div>
